# This CMakeLists.txt is provided for reference only.
# The firmware is intended to be built with PlatformIO or Arduino IDE.
cmake_minimum_required(VERSION 3.16)
project(esp32_ros2_motor)

# This is not a ROS‑package, just a placeholder.
message(STATUS "Firmware source files are located here.")
